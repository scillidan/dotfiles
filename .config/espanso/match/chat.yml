matches:
  - trigger: ":what"
    replace: "Using metaphors and rhetoric helps me understand \"What is $|$\""

  - trigger: ":diff"
    replace: "What is the difference between "

  - trigger: ":match"
    replace: "Used regex, how to match string `$|$`"

  - trigger: ":aut"
    form: |
      Authors: [[author]]🧙‍♂️, scillidan🤡
    form_fields:
      author:
        type: choice
        values:
          - GPT-4o mini
          - DeepSeek
          - mistral.ai
          - perplexity.ai
        default: GPT-4o mini

  - trigger: ":by"
    form: |
      [[by]]
    form_fields:
      by:
        type: choice
        values:
          - '[perplexity.ai]'
          - '[GPT-4o mini]'
        default: '[perplexity.ai]'
